/**
 * Shared contract markers for the three frontend apps.
 * The repo is still JavaScript-first, so these are runtime constants plus JSDoc anchors.
 */

export const SELF_STATUS = {
  IN: 'IN',
  OUT: 'OUT'
};

export const SCAN_ACTION = {
  CHECKED_IN: 'checked_in',
  CHECKED_OUT: 'checked_out'
};
