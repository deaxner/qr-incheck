route:
  receiver: qr-incheck-webhook
  group_by: ['alertname', 'service']
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 1h

receivers:
  - name: qr-incheck-webhook
    webhook_configs:
      - url: http://alert-receiver:8080/alerts
        send_resolved: true
